jobs:
  include:
  - name: gcc, pg 9.5
    language: c
    services: docker
    sudo:     required
    before_script:
    - docker build --tag fs2pg --file Dockerfile .
    - docker run -d fs2pg
    script:
    - docker ps
    - docker exec -i fs2pg which psql
    after_script:
    - docker rm --force fs2pg
